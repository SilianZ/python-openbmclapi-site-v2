---
title: Installation
sidebar_position: 1
---

```mdx-code-block
import Tabs from "@theme/Tabs"
import TabItem from "@theme/TabItem"
import Asciinema from "@site/src/components/Asciinema";
```

If you want to use Docker, go to [Use Docker](/docs/deploy-with-docker).

## Install manually {#install-manually}

1. Install [Python](https://www.python.org/) on your machine.

   :::info

   Recommended Python version：3.10+.

   :::

2. Download source:

   ```mdx-code-block
   <Tabs>
   <TabItem value="c1" label="下载 Release" default>
   ```

   1. Download source from [Releases](https://github.com/TTB-Network/python-openbmclapi/releases).

   2. Put downloaded source code into a pre-prepared folder.

   3. Open terminal in folder.

   ```mdx-code-block
   </TabItem>
   <TabItem value="c2" label="使用 Git 克隆">
   ```

   1. Clone repository:

      ```sh
      git clone https://github.com/TTB-Network/python-openbmclapi.git
      ```

      :::tip

      You can also speed up with mirror sources:

      ```sh
      git clone https://ghproxy.bugungu.top/https://github.com/TTB-Network/python-openbmclapi.git
      ```

      :::

   2. Open repository folder:

      ```sh
      cd python-openbmclapi
      ```

   ```mdx-code-block
   <Asciinema
   url="https://asciinema.org/a/655199.cast"
   options={{
       theme: "monokai",
       poster: "npt:21.5",
       cols: 100,
       rows: 30,
       idleTimeLimit: 0.2,
   }}
   />

   </TabItem>
   </Tabs>
   ```

3. Install dependencies:

   ```sh
   pip install -r requirements.txt
   ```

   :::tip

   You may need to install [Microsoft Visual C++ Build Tools](https://visualstudio.microsoft.com/visual-cpp-build-tools/).

   :::

   ```mdx-code-block
   <Asciinema
   url="https://asciinema.org/a/655200.cast"
   options={{
       theme: "monokai",
       poster: "npt:21.5",
       cols: 100,
       rows: 30,
       idleTimeLimit: 0.2,
   }}
   />
   ```
